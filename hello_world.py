"""
เกี่ยวกับการใช้ Whitespace (ช่องว่าง) ในการจัดรูปแบบข้อความ:

- Whitespace คือ อักขระที่ไม่ได้ถูกพิมพ์ออกมา เช่น Space (ช่องว่าง), Tab (แท็บ), Newline (ขึ้นบรรทัดใหม่)
- ใช้ `\t` เพื่อเพิ่ม Tab ในข้อความ ทำให้ข้อความถูกเยื้องเข้ามา
- ใช้ `\n` เพื่อเพิ่ม Newline ในข้อความ ทำให้ข้อความขึ้นบรรทัดใหม่
- การใช้ Whitespace ช่วยจัดระเบียบและทำให้ข้อความอ่านง่ายขึ้น

การใช้ `\t` และ `\n` ช่วยให้เราสามารถควบคุมการแสดงผลข้อความได้ตามต้องการ
"""

print("Languages:\nPython\nC\nJavaScript")
print("\tPython")